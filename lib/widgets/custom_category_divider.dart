import 'package:flutter/material.dart';

class CustomCategoryDivider extends StatelessWidget {
  const CustomCategoryDivider({super.key});

  @override
  Widget build(BuildContext context) {
    return const Divider(
      color: Colors.black87,
      height: 2.0,
    );
  }
}
